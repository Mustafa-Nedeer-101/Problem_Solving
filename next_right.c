#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <string.h>
#include <ctype.h>


struct Node {
  int val;
  struct Node *left;
  struct Node *right;
  struct Node *next;
};


struct Node* connect_rec(struct Node* root, struct Node* next) {
    // Base case
    if (root == NULL) {
        return NULL;
    }

  return NULL;
}


struct Node* connect(struct Node* root) {
	
}

int main(void) {


    return 0;
}